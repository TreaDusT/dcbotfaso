module.exports.run = async (bot, message, args) => {
      message.reply('s+')
}

module.exports.config = {
    name: "aktif",
    description: "aktif",
    usage: ".aktif",
    accessableby: "MANAGE_ROLES",
    aliases: []
}